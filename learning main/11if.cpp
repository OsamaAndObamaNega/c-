#include <iostream>
#include <cmath>

int main()
{
    int age;

    std::cout<<"enter ur age: ";std::cin>>age;

    if(age>=100){
        std::cout<<"u are to old to be alive and ur tring to whtch porn? nigga i can bet u where a slave not a onwer u where a gray sale gigga";
    }

    else if(age >=13){
        std::cout<<"welcom to the site";
    }
    else if(age<0){
        std::cout<<"u have't been born yet";

    }

    else{
        std::cout<<"sorry u r not allowed to enter u have to be 13 atlist to enter or older then 13 fucku";
    }
    
    return 0;
}

