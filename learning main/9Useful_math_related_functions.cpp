#include <iostream>
#include <cmath>

int main()
{
    double x = 3.14;
    double y = 4.99;
    double z;

    // z = std::max(x,y);
    // z = std::min(x,y);
    // z = pow(x,y);
    // z = sqrt(9);
    // z =abs(-99);
    // z = round(x);
    y = floor(y);


    
    std::cout<<y;


    return 0;
}

