#include <iostream>
#include <cmath>

int main()
{
    int num =9;
    // num %2 ? std::cout<<"the num is ODD " : std::cout<<"the num is ficking EVEN";

    std::cout<<(num %2 ? "this is an fucking --ODD-- number" : "this is an --EVEN-- number");


    return 0;
}
