#include <iostream>

int main(){
    int A = 40;

    A >= 50 ? std::cout<<"the A is grater then or ecluse to  50 \n" : std::cout <<"the A is not grater or less then 50\n"; 
    
    bool pass = true;
    pass ? std::cout <<"good for u" : std::cout<<"bar for u";

    return 0;
}

